# VRAG bağımlılıkları (Python 3.12+)
#
# GPU (CUDA) kullanımı için torch'u CUDA index'inden kurun, örn:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# transformers, SigLIP2 görüntü encoder'ı için gerekir.
torch>=2.2
transformers>=4.49
qdrant-client>=1.10
pillow>=10.0
numpy>=1.26

# Web arayüzü (app.py)
flask>=3.0
